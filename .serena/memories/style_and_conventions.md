Repo uses ESM TypeScript with `strict` mode. Formatting/linting comes from oxfmt/oxlint with single quotes, semicolons, and `printWidth: 100`. Follow existing JSDoc style on exported APIs. Keep implementation source of truth in `src/`; if public API changes, manually update `types/autokana.d.ts` because `tsconfig.json` only includes `src` and `__tests__`. Avoid hand-editing generated `dist/` outputs.